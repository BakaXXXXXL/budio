# biliaudio 配置文件
# 首次运行时会自动复制到 ~/.config/biliaudio/config.toml

[cookie]
# Cookie 文件路径（Netscape 格式）
# 留空则不使用 Cookie，启动时询问
path = ""

[download]
# 默认下载目录（留空则使用当前目录）
output_dir = ""

# 默认音频格式: mp3, aac, flac, m4a, opus, wav
audio_format = "mp3"

# 默认视频格式: mp4, mkv, webm
video_format = "mp4"
